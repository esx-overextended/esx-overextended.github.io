---
sidebar_label: /coords
---

# Coords

This command prints the coordinates of the player that executed this command in `vector3` and `vector4` format, in both client & server console.

```lua
/coords
```

:::info
_By default only admins can execute this command_
:::
