---
sidebar_label: /setduty
---

# Set Duty

This command sets the job's duty-state of the player with the specified server id.

```lua
/setduty [id] [duty]
```

- id: `number` (the server id of the target player)
- duty: `true` or `false`

:::info
_By default only admins can execute this command_
:::
