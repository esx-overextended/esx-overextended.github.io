---
sidebar_label: /freeze
---

# Freeze

This command freezes the player with the spacified id so that they cannot move.

```lua
/freeze [id]
```

- id: `number` (the server id of the target player)

:::info
_By default only admins can execute this command_
:::